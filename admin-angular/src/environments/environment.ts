// Configuración de entorno. Para producción, edita apiUrl con la URL pública
// del backend en Render (p. ej. https://mi-backend.onrender.com/api).
export const environment = {
  production: false,
  apiUrl: 'http://localhost:4000/api',
};
